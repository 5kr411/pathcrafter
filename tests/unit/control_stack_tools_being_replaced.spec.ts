import { CollectorControlStack } from '../../bots/collector/control_stack';

describe('CollectorControlStack', () => {
  it('exposes toolsBeingReplaced as a readable Set', () => {
    expect(typeof CollectorControlStack).toBe('function');
  });
});
