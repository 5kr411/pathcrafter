/**
 * Render item/block names for display
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any -- plugin-data untyped
export function renderName(name: string, _meta?: any): string {
  return name;
}

